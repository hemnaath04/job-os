{
  "name": "@job-os/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "job.os application autofill. Fills only verified profile facts, never submits.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/**/*.test.ts\""
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/jsdom": "^21.1.7",
    "@types/node": "^22.19.21",
    "esbuild": "^0.28.2",
    "jsdom": "^30.0.1",
    "typescript": "^5.7.0"
  }
}
